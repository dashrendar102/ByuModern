<Page
    x:Name="pageRoot"
    x:Class="BYU.ClassesPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BYU"
    xmlns:data="using:BYU.Data"
    xmlns:common="using:BYU.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Background="#FF1D1D1D"
        DataContext="{Binding Item}"
        d:DataContext="{Binding Groups[0].Items[0], Source={d:DesignData Source=/DataModel/SampleData.json, Type=data:SampleDataSource}}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--
            TODO: Content should be placed within the following grid 
                  to show details for the current item
        -->
    <!--
            TODO: Content should be placed within the following grid 
                  to show details for the current item
        -->
        <Grid Grid.Row="1" x:Name="contentRegion" Margin="0,10,0,-10" Background="#FFB0B0B0">
        	<ListBox x:Name="classList" HorizontalAlignment="Left" Height="608" Margin="91,-2,0,0" VerticalAlignment="Top" Width="488" Background="Transparent">
                <Button Content="AH 100" Height="82" Width="465" FontSize="32" Background="#FF002255" Click="ClassClick"/>
                <Button Content="C S 428" Height="82" Width="466" FontSize="32" Background="#FF002255" Click="ClassClick"/>
                <Button Content="BIO 100" Height="82" Width="469" FontSize="32" Background="#FF002255" Click="ClassClick"/>
                <Button Content="MUSIC 117" Height="82" Width="467" FontSize="32" Background="#FF002255" Click="ClassClick"/>
                <Button Content="WRTG 150" Height="82" Width="467" FontSize="32" Background="#FF002255" Click="ClassClick"/>
        	</ListBox>
        	<GridView x:Name="selectedClassContent" HorizontalAlignment="Left" Height="628" Margin="582,0,0,0" VerticalAlignment="Top" Width="450" Background="Gray">
        		<TextBlock x:Name="selectedClassTitle" Height="80" TextWrapping="Wrap" Width="300" FontSize="32" Margin="15,15,0,0"/>
        	</GridView>
        </Grid>

        <!-- Back button and page title -->
        <Grid x:Name="classesPageHeader">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="My Classes" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
    </Grid>
</Page>
