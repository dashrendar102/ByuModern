<Page
    x:Name="pageRoot"
    x:Class="BYU.HubPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BYU"
    xmlns:data="using:BYU.Data"
    xmlns:common="using:BYU.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- Grid-appropriate 310 by 260 pixel item template as seen in section 4 -->
        <DataTemplate x:Key="Standard310x260ItemTemplate">
            <Grid Height="250" Width="310" Margin="5,10,5,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" Height="150">
                    <Image Source="{Binding ImagePath}" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}"/>
                </Border>
                <StackPanel Grid.Row="1" Margin="0,10,0,0">
                    <TextBlock Text="{Binding Title}" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="NoWrap"/>
                    <TextBlock Text="{Binding Description}" Style="{StaticResource BodyTextBlockStyle}" MaxHeight="60" />
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <!--
    This grid acts as a root panel for the page.
  -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <!-- Define Backgrounds -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" Background="#FF002255"/>

        <Border Grid.Row="1" Grid.ColumnSpan="3">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FFE0EDF9" Offset="1"/>
                    <GradientStop Color="White"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- button and page title -->
        <Button  x:Name="backButton" Style="{StaticResource NavigationBackButtonNormalStyle}"
                Margin="20,42,0,0" 
                VerticalAlignment="Top"
                Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                AutomationProperties.Name="Back"
                AutomationProperties.AutomationId="BackButton"
                AutomationProperties.ItemType="Navigation Button" HorizontalAlignment="Left" Width="41"/>
        <Image Source="Assets/Logos/logo_150_150.png" Grid.Column="1"/>
        <Button x:Name="SignInButton" Content="Sign In" Grid.Column="2" HorizontalAlignment="Right" Margin="0,0,30,0" Click="SignIn_Click"/>
        <ScrollViewer VerticalScrollBarVisibility="Hidden" VerticalScrollMode="Disabled" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Hidden" Grid.Row="2" Grid.ColumnSpan="3">
            <StackPanel Orientation="Horizontal">
                <HubSection Width="362" x:Uid="ClassesHeader" Header="My Classes">
                    <DataTemplate>
                        <StackPanel>
                            <Button Margin="0,0,0,13" BorderThickness="0" Height="80" Width="294" Click="ClassButton_Click">
                                <Image Source="Assets/class_demo.jpg"/>
                            </Button>
                            <Button Margin="0,0,0,13" BorderThickness="0" Height="80" Width="294"  Content="Enter Brian's Demotopia" Click="BergerHyperlinkButton_Click"/>
                        </StackPanel>
                    </DataTemplate>
                </HubSection>
                <HubSection x:Uid="MapHeader" Header="Where on campus" Padding="40,40,40,32" Width="428">
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Row="0" Grid.Column="0" BorderThickness="0" Click="MapButton_Clicked">
                                <Image Source="Assets/campus_buildings.jpg"/>
                            </Button>
                            <Button Grid.Row="0" Grid.Column="1" BorderThickness="0" Click="MapButton_Clicked">
                                <Image Source="Assets/blank_map_tile.jpg"/>
                            </Button>
                            <Button Grid.Row="1" Grid.Column="0" BorderThickness="0" Click="MapButton_Clicked">
                                <Image Source="Assets/blank_map_tile.jpg"/>
                            </Button>
                            <Button Grid.Row="1" Grid.Column="1" BorderThickness="0" Click="MapButton_Clicked">
                                <Image Source="Assets/parking_lots.jpg"/>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </HubSection>
                <HubSection x:Uid="Map2Header" Header="Where on campus, version 2">
                    <DataTemplate>
                        <!-- width of 400 -->
                        <StackPanel Orientation="Vertical">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="130"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="270"/>
                                    <RowDefinition Height="95"/>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Image Source="Assets/MediumGray.png" Grid.ColumnSpan="5" Margin="0,0,0,10" Stretch="Fill" />
                                <Button Grid.Row="1" Grid.Column="0" BorderThickness="0" Click="MapButton_Clicked">
                                    <Image Source="Assets/campus_buildings.jpg"/>
                                </Button>
                                <Button Grid.Row="1" Grid.Column="2" BorderThickness="0" Click="MapButton_Clicked">
                                    <Image Source="Assets/blank_map_tile.jpg"/>
                                </Button>
                                <Button Grid.Row="1" Grid.Column="4" BorderThickness="0" Click="MapButton_Clicked">
                                    <Image Source="Assets/parking_lots.jpg"/>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </DataTemplate>
                </HubSection>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
